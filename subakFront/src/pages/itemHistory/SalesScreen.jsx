import { Text } from "react-native";
import { shared } from "../../styles/shared";

const SalesScreen = () => {
  return (
    <Text style={shared.text}>SalesScreen</Text>
  );
};

export default SalesScreen;