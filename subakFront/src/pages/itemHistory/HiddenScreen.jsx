import { Text } from "react-native";
import { shared } from "../../styles/shared";

const HiddenScreen = () => {
  return (
    <Text style={shared.text}>HiddenScreen</Text>
  );
};

export default HiddenScreen;