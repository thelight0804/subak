import { Text } from "react-native";
import { shared } from "../../styles/shared";

const CompletedScreen = () => {
  return (
    <Text style={shared.text}>CompletedScreen</Text>
  );
};

export default CompletedScreen;